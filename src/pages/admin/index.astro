---
import Layout from "@layouts/Layout.astro";
import Login from "@containers/login/index.astro";

const isAuthenticated = Astro.cookies.get("auth")?.json() || false;

if(isAuthenticated) {
	return Astro.redirect('./admin/dashboard');
}
---

<Layout title="Admin">
	<Login />
</Layout>
